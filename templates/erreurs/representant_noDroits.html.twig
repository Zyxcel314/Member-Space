{% extends "base.html.twig" %}
{% block title %} Absence des droits - Odyssée du cirque {% endblock %}

{% block body %}
    <div>
        <h2 class="mx-5 my-3"> Espace représentant </h2>
        {% include 'front_office/representant_famille/espaceRepresentant.html.twig' %}
    </div>
    <div class="row my-3 mx-5 shadow-lg bg-white">
        <div class="m-3 w-100">
            <h3 class="text-danger"> Cher {{ representant.nom }} {{ representant.prenom }}, vous ne pouvez pas {{ nomOperation }} les informations du dossier de {{ membre.nom }} {{ membre.prenom }} ! </h3>
            <form class="mr-3 d-inline" action="{{ path("Representant.accueil") }}">
                <button type="submit" class="btn btn-secondary"> Retourner à votre espace </button>
            </form>
        </div>
    </div>
{% endblock %}