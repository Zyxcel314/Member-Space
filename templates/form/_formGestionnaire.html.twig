{{ form_row(form.nom, {'label': 'Nom : '}) }}
{{ form_row(form.prenom, {'label': 'Prénom : '}) }}
{{ form_row(form.mail, {'label': 'Mail : '}) }}
{{ form_row(form.motDePasse, {'label': 'Mot de passe : '}) }}
{{ form_row(form.confimerMDP, {'label': 'Confimez le mot de passe : '}) }}
{{ form_row(form.idGoogleAuth, {'label': 'ID Google Authentification : '}) }}
<button class="btn btn-primary"> {{ button_label | default('Confirmer') }} </button>
<input type="hidden" name="token" value="{{ csrf_token('gestionnaire') }}"/>