{{ form_row(form.mail, {'label': 'Adresse mail : '}) }}
{{ form_row(form.communicationResponsableLegal, {'label': 'Ce majeur autorise-t-il la diffusion de son image ? '}) }}
{{ form_row(form._token) }}
<button class="btn btn-primary"> {{ button_label | default('Confirmer') }} </button>
<input type="hidden" name="token" value="{{ csrf_token('infosMajeur') }}"/>