<form method="post" action="{{ path('Representant.supprimer', {'id': representant_famille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ representant_famille.id) }}">
    <button class="btn">Delete</button>
</form>
