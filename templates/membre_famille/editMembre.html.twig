{% extends "base.html.twig" %}
{% block body %}
    <div class="container">
        <br>
        <div class="row">
            {% if is_granted('ROLE_ADMIN') %}
                <form action="{{ path("Gestionnaire.showMembres") }}">
                    <button type="submit" class="btn btn-primary"> Revenir à la liste des membres </button>
                </form>
            {% elseif is_granted('ROLE_USER') %}
                <form action="{{ path("Membre.showMembres") }}">
                    <button type="submit" class="btn btn-primary"> Revenir à la liste des membres </button>
                </form>
            {% endif %}
        </div>
        <br>
        {% if is_granted('ROLE_ADMIN') %}
            <form method="POST" action="{{ path('Gestionnaire.validEditMembre') }}">
                <legend> Modifier les informations concernant un membre de cette famille </legend>
                <br>
                {{ include('membre_famille/_formMembre.html.twig', {button: 'Modifier'}) }}
            </form>
        {% elseif is_granted('ROLE_USER') %}
            <form method="POST" action="{{ path('Membre.validEditMembre') }}">
                <legend> Modifier les informations concernant un membre de votre famille </legend>
                <br>
                {{ include('membre_famille/_formMembre.html.twig', {button: 'Modifier'}) }}
            </form>
        {% endif %}
    </div>
{% endblock %}