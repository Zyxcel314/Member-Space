{% extends 'base.html.twig' %}
{% block title %} Ajouter un employeur - Odyssée du cirque {% endblock %}

{% block body %}
    <div>
        <h2 class="mx-5 my-3"> Membres de votre famille </h2>
        {% include 'front_office/representant_famille/espaceRepresentant.html.twig' %}
    </div>
    <div class="mx-5 my-3 shadow-lg p-3 bg-white rounded">
        <h3 class="w-100"> Ajouter un employeur au responsable {{ representant.nom }} {{ representant.prenom }} </h3>
        <div class="my-3">
            <form class="mr-3 d-inline" action="{{ path("InfosEmployeur.show", {id: membre.id, idResponsable: responsable.id}) }}">
                <button type="submit" class="btn btn-secondary"> Retourner aux informations de l'employeur </button>
            </form>
        </div>
        {{ form_start(form) }}
        {{ include('form/_formInfosEmployeur.html.twig', {'button_label': 'Ajouter'}) }}
        {{ form_end(form) }}
    </div>
{% endblock %}