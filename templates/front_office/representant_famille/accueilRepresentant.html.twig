{% extends "base.html.twig" %}

{% block body %}
    <h2 class="mx-5 my-3"> Mon espace </h2>
    <ul class="nav nav-tabs pl-5">
        <li class="nav-item">
            <a class="nav-link active" href="{{ path('Representant.InfosPerso.show') }}"> Informations personnelles </a>
            <input type="hidden" name="token" value="{{ csrf_token('infosPerso.show') }}">
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="{{ path('InfosFamille.show') }}"> Informations familliales </a>
            <input type="hidden" name="token" value="{{ csrf_token('infosFamille.show') }}">
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="{{ path('Membre.show') }}"> Membres de ma famille </a>
            <input type="hidden" name="token" value="{{ csrf_token('membre.show') }}">
        </li>
    </ul>
{% endblock %}